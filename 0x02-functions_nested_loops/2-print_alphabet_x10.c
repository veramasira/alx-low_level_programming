#include "main.h"
/**
 * print_alphabet_x10 - function to print abc 10times
 * Return: 0
 */
void print_alphabet_x10(void)
{
	int t = 0;
	char ch;

	while (t < 10)
	{
		char = 'a',
		     while (ch <='z')
		     {
			     _putchar(ch);
			     ch++;

		     }
		_putchar('\n')
			 t++;
	}

